package it.stapedev.enums;

public enum EGosseGamePanel {
    GOOSE, // with this kind of panel, the user can move again
    BRIDGE, // with this kind of panel, the user can move on panel 12
    NORMAL, // panel with no special behaviour
    ;
}
